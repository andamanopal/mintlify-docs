<Note>
  All API requests require authentication using a Bearer token. Obtain a token by authenticating through the [Login API](/api-reference/auth/login).
  
  Include the token in the Authorization header of all requests:
  ```
  Authorization: Bearer {your_token}
  ```
</Note> 